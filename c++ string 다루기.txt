to_string함수로 다른 자료형을 string으로 변환가능
+로 두 문자열을 붙일 수 있다 (append로도 가능한데 굳이 쓸 일 없을듯?)
<string>헤더의 string.substr(시작 index, 길이)로 문자열의 특정 부분을 추출할 수 있다.
<string>헤더의 string.find()로 부분문자열의 포함 여부를 확인할 수 있다.
  이 메소드는 실패할 경우 string::npos 상수를 리턴한다.